## COMPLETENESS AUDIT

The core pipeline is delivered. The gaps are almost entirely presentation, one spec/implementation priority mismatch, and one submission-text claim that must be corrected to preserve the project's honesty rule.

| Claim | Status | Evidence |
| --- | --- | --- |
| A rebuild makes the bounded claim “N of M observed behaviors, X% branch coverage,” not a claim about unobserved inputs. | delivered | `README.md:3`; the emitted SOUL fixes the same wording at `src/distill/engines.ts:44-45`; terminal and report show both values at `src/resurrect/command.ts:43-55` and `src/report/html.ts:31,60-65`. The inspected `is-odd` artifact is concrete evidence: `.necromancer-cache/probes/is-odd-3.0.1-upfObP/SOUL.md:5` and `graveyard.html:24`. |
| Six phases: triage, isolated runner, deterministic probe, SOUL + test, model-gated rebuild, static report. | delivered | Phase commands and chaining are implemented in `src/cli.ts:48-98` and `src/resurrect/command.ts:69-123`; deterministic double execution is `src/probe/index.ts:61-78`; the report is emitted at `src/report/stage.ts:29-52`. |
| The Soul is a cited, executable characterization artifact that is valuable even without adoption of the rebuild. | delivered | Each behavior becomes a test in `src/distill/test-template.ts:177-190`; required evidence IDs are validated at `src/distill/engines.ts:51-68`; the inspected artifact contains both the cited SOUL and its 67-test suite at `.necromancer-cache/probes/is-odd-3.0.1-upfObP/SOUL.md:5-128` and `soul.test.ts:873-887`. |
| PROBE combines exports, README/tests, semantic plans, fuzz amplification, a determinism filter, and original coverage. | delivered | Discovery is `src/probe/discovery.ts:121-149`; model/heuristic plans merge with deterministic fast-check samples at `src/probe/candidates.ts:132-174`; coverage is collected from the original at `src/probe/coverage.ts:15-62`. |
| Rebuild from the SOUL/test/public API only, with no original-package source in the model work order; modern strict TypeScript with dual ESM/CJS output and no runtime dependencies. | delivered | The work order is deliberately limited at `src/resurrect/engines.ts:30-43,63-70`; original references are rejected at `src/resurrect/build.ts:13-21`; the generated manifest and dual builds are defined at `src/resurrect/build.ts:24-76,129-137`. |
| Scanner-backed before/after advisory evidence, LOC, and dependency deltas. | partial | The delivered implementation performs bounded OSV queries and labels outages as unknown (`src/report/osv.ts:19-31`, `src/report/html.ts:14-24`), then reports LOC/dependency deltas (`src/report/html.ts:68-72`). The spec says “npm audit/OSV” (`SPEC.md:56-58`), but only OSV is implemented. More importantly for the demo, the report preserves only counts, not advisory identities or scan scope (`src/report/osv.ts:6-16`). |
| Codex is the default resurrection engine, with API as fallback. | partial | This is the architecture promise in `SPEC.md:51-54,65` and is essential to the Codex story. In the current implementation, automatic mode instead chooses API first whenever a key exists (`src/resurrect/engines.ts:129-141`); README accurately documents that changed behavior at `README.md:25-27`. The demo command also omits `--engine codex` (`SUBMISSION_VIDEO_SCRIPT.md:15`). |
| A polished terminal and a tombstone-to-phoenix moment. | partial | The report has a restrained tombstone header and fidelity meter (`src/report/html.ts:41-57`), but no phoenix/evidence transition. The terminal is only a static banner and phase line (`src/terminal.ts:1-11`): no spinner, elapsed/progress feedback, round ledger, engine provenance, or next-action handoff, despite `SPEC.md:58,70`. |
| Python is explicitly teased as a future direction in the README. | missing | The spec asks for a Python v2 tease at `SPEC.md:29`; README only lists Python as out of scope at `README.md:42-51`. |
| Devpost says resurrection has a deterministic heuristic fallback “so the tool always produces a result.” | missing / incorrect claim | `SUBMISSION_DEVPOST.md:27` conflicts with the correct product contract in `README.md:27` and the explicit failure path in `src/resurrect/engines.ts:129-142`. RESURRECT must not gain this fallback; the submission text must lose it. |

## CREATIVITY GAPS (ranked)

1. **Visible resurrection ledger and engine provenance.** Print the selected engine, then show each bounded rebuild round as `round 2/6 — 54 of 67 observed behaviors passing`, including build/test failure status. The current loop has the data but emits no callbacks (`src/resurrect/loop.ts:105-123`). This makes the audience watch a proof converge rather than wait behind a phase label, and visibly establishes whether Codex actually performed the rebuild. **Effort:** M. **Suite risk:** low. **Demo payoff:** high.

2. **Turn `graveyard.html` into an evidence-first case file, not a metrics dashboard.** Make the tombstone lead into three explicit facts: observed-suite reproduction, original branch coverage, and the boundary “unobserved behavior is not claimed.” Then show a compact evidence chain: original package → recorded Soul/test → rebuilt package. The current report has the numbers but its only narrative body is a scrollable raw cluster dump (`src/report/html.ts:7-12,74-75`). That is the visual that can make judges remember NECROMANCER as the rare AI tool that exposes its proof. **Effort:** M. **Suite risk:** low. **Demo payoff:** high.

3. **Add a scanner-backed deep-extend security exhibit.** Preserve the exact advisory/CVE identifiers returned by OSV for the selected original version, show the package/version that was scanned, and contrast it with the rebuilt manifest’s declared dependency scan. For zero dependencies, say exactly that no advisory search was needed across 0 declared runtime dependencies; for unavailable OSV, say unknown. This gives the real `deep-extend` story a forensic receipt instead of a generic “before/after” count. **Effort:** M. **Suite risk:** medium. **Demo payoff:** high.

4. **Preflight model availability before downloading the corpse.** `resurrect` currently fetches and triages first (`src/resurrect/command.ts:72-84`) and only then tells a judge that no API key/Codex CLI is available. Put a brief, explicit preflight before EXHUME: identify the selected engine or print two copyable setup paths plus the useful no-model alternatives, `probe` and `distill`. It protects the first 10 seconds of a cold judge run. **Effort:** S. **Suite risk:** low. **Demo payoff:** high.

5. **Make source quarantine a first-class proof card.** The most original fact is not “AI rewrote code”; it is that the rebuild receives the SOUL, test, public API, and failure feedback, while the original source is withheld (`src/resurrect/engines.ts:30-43`). Put that exact four-item contract in the report and terminal summary. Judges can instantly distinguish this from a source-to-source rewrite, and the statement is mechanically true. **Effort:** S. **Suite risk:** none. **Demo payoff:** high.

6. **Feature one deterministic Quirk, with its evidence ID, instead of burying Quirks.** The demo script correctly calls for a Quirk (`SUBMISSION_VIDEO_SCRIPT.md:21-28`), yet the HTML renderer explicitly stops before `## Quirks` (`src/report/html.ts:7-12`). Deterministically select a recorded unusual property-key, coercion, or exact-error case; render input, output/throw, and evidence ID. If none exists, render “No quirk was recorded” rather than inventing drama. **Effort:** S. **Suite risk:** low. **Demo payoff:** high.

7. **Publish a release-time empirical range card.** Add a compact, generated-from-artifacts table for the fresh ten-package sweep: package@version, observed-behavior count, original branch coverage, and result; include rimraf’s exact OUT_OF_SCOPE reason as a positive guardrail. It converts “arbitrary small packages” from a promise into a judge-readable range result without pretending the sample proves all npm. **Effort:** S. **Suite risk:** none. **Demo payoff:** high.

8. **Ship a clearly labelled recorded-run replay capsule for the video.** Package a tiny static `is-odd`/`deep-extend` report bundle with metadata such as package version, engine, behavior count, coverage, and scan status, and label it “recorded run — not a live invocation.” This gives the three-minute video a reliable transition from live phases to a rich artifact if model latency is variable, exactly as the script anticipates (`SUBMISSION_VIDEO_SCRIPT.md:3`). **Effort:** M. **Suite risk:** none. **Demo payoff:** medium.

9. **Give every completed command a reproducibility handoff.** After probe/distill/resurrect, print three platform-neutral, copyable next actions: view `SOUL.md`, retest the original, and retest `NECROMANCER_IMPL=rebuilt`. Today the CLI prints an artifact path and bare “Available” phase labels (`src/cli.ts:90-94`, `src/resurrect/command.ts:43-55`), while the useful retest command is hidden in README (`README.md:106-110`). This makes the Soul’s independent value visible to a judge who stops after one command. **Effort:** S. **Suite risk:** none. **Demo payoff:** medium.

10. **Use two semantically distinct meters, never a single green “success” bar.** The current 100%-wide meter represents only reproduction of the recorded suite (`src/report/html.ts:27-31,43,55-57`), while original branch coverage appears later. Make the labels and geometry visibly separate: “observed suite reproduced” can reach 67/67; “original code exercised” remains 71.42%. This turns the honest-claims rule into a memorable visual language instead of a disclaimer. **Effort:** S. **Suite risk:** low. **Demo payoff:** high.

## DEMO-KILLER CHECK

No raw-stack-trace path is apparent in the top-level CLI: unexpected errors are converted to a message at `src/cli.ts:171-176`, and the missing-model path has a dedicated exit code/message at `src/resurrect/command.ts:82-91`. The following are still capable of making a good tool look less finished on a judge’s first run.

| Severity | Cold-run / video failure | Evidence | Required product response |
| --- | --- | --- | --- |
| High | **It looks hung during the most important moment.** A model request may wait 90–120 seconds and the loop can make six attempts, yet the terminal emits only the initial RESURRECT phase. A judge cannot distinguish normal generation, TypeScript build, test execution, or a stalled run. | `src/resurrect/engines.ts:50-58,84-103`; `src/resurrect/loop.ts:105-123`; `src/terminal.ts:5-7` | Emit selected engine, round start/end, current observed count, and clear “generating / building / testing” transitions. Never show an unsupported parity percentage. |
| High | **The Codex requirement may be invisible in the submitted video.** The script runs auto mode, but auto prefers API when a key is present; terminal summary does not print the generator used. A Stage One reviewer could reasonably see an OpenAI API demo rather than a Codex-powered rebuild. | `SUBMISSION_VIDEO_SCRIPT.md:15`; `src/resurrect/engines.ts:129-142`; `src/resurrect/command.ts:108-120` | Use `--engine codex` in the filmed live command when authenticated, print the actual engine, and make auto-mode precedence match the intended submission story or document it plainly. |
| High | **The deep-extend win is not legible from the report.** The renderer shows aggregate advisory counts but no advisory identifier, queried package/version, or declared scan boundary. A “1 → 0” voiceover therefore asks the judge to trust an unexplained number. | `src/report/osv.ts:6-16,19-31`; `src/report/html.ts:14-24,64-65` | Render OSV-returned identifiers and scope only; retain `unknown, OSV unreachable` exactly when appropriate. Do not add a “CVE-free” badge. |
| Medium | **A missing key/auth check happens after network work.** On a newly installed CLI with neither credential path, RESURRECT downloads and triages the package before it can provide the setup instruction. That feels like an avoidable failed run and leaves no artifacts for the user. | `src/resurrect/command.ts:72-97`; `src/resurrect/engines.ts:129-142` | Preflight first and offer copyable API/Codex setup plus `probe`/`distill` as meaningful no-model next steps. |
| Medium | **The report’s strongest artifact is visually a long raw text box.** It hides Quirks and makes the report awkward to point at on a screen recording; the current `is-odd` artifact visibly requires scrolling through raw behavior rows. | `src/report/html.ts:7-12,74-75`; `.necromancer-cache/probes/is-odd-3.0.1-upfObP/graveyard.html:43-125` | Promote a deterministic cited Quirk and compact evidence chain above the fold; keep the full SOUL available below it. |
| Medium | **The fully green observed-suite meter can be misread as full package coverage.** Its displayed 100% bar is semantically correct, but the 71.42% original branch coverage is visually secondary in the inspected `is-odd` report. | `src/report/html.ts:29-31,43,55-65`; `.necromancer-cache/probes/is-odd-3.0.1-upfObP/graveyard.html:24-32` | Use a labelled observed-suite count and a separate coverage treatment with an explicit unobserved-behavior boundary. |
| Low | **The reduced-isolation warning is accurate but reads like a failure to a hurried judge.** It appears during every coverage-enabled probe, without a short confirmation that this is expected v1 behavior. | `src/sandbox/index.ts:371-390`; `README.md:53-55` | Retain the warning verbatim in substance, prefix it as an expected v1 safety notice, then print a normal “sandbox ready; probing with local V8 coverage” transition. |

## TOP 3 TO BUILD

1. **Live resurrection ledger with truthful Codex provenance** — touch `src/resurrect/types.ts`, `src/resurrect/loop.ts`, `src/resurrect/command.ts`, `src/terminal.ts`, the targeted resurrection tests, `README.md`, `SUBMISSION_VIDEO_SCRIPT.md`, and `SUBMISSION_DEVPOST.md`. Add an optional loop event callback that reports engine selection, round start, generation complete, build/test start, and a completed-round result of the exact form `X of Y observed behaviors`. The CLI must print the generator actually used, not merely requested; when both engines are available, make automatic precedence consistent with the intended Codex-default submission story and retain API fallback if Codex generation fails. Film `--engine codex` explicitly. Correct the Devpost heuristic-fallback sentence: RESURRECT requires a model and must say so. Acceptance: six-round cap, exit 3 for incomplete observed suites, exit 4 for no engine, artifact/result formats, API fallback, and every bounded-fidelity phrase remain intact; no progress message may say “identical,” “100% parity,” or imply coverage not measured.

2. **Evidence-first graveyard report** — touch `src/report/html.ts` and its targeted report-render tests. Keep the report fully static and dependency-free, but redesign the first viewport as a “resurrection receipt”: package@version; separate cards for `X of Y observed behaviors reproduced` and `Z% branch coverage of the original`; a one-sentence boundary that no unobserved behavior is claimed; the actual rebuild engine; a source-quarantine card listing only SOUL, characterization suite, public API shape, and failing observations as rebuild inputs; and one deterministic cited Quirk extracted from `## Quirks` or an honest no-quirk fallback. Keep the full SOUL clusters below the fold and add the Quirks content rather than cutting it. Acceptance: all interpolated package/SOUL/OSV text remains escaped, `REVIVED` remains only a visual threshold state, zero/unknown OSV wording stays accurate, and existing report-number semantics remain unchanged.

3. **Scanner-backed security receipt for deep-extend** — touch `src/report/types.ts`, `src/report/osv.ts`, `src/report/stage.ts`, `src/report/html.ts`, `src/resurrect/command.ts`, and focused OSV/report tests. Extend parsed OSV evidence to retain only identifiers actually returned for the queried package/version (OSV ID and CVE aliases where present), alongside the existing counts and status. Render a scan-scope line for both sides: `original: <name>@<version>, queried at report time`; `rebuild: <N> declared runtime dependencies scanned`; for N=0, state that the measured result is no advisories found across 0 declared runtime dependencies. On timeout/error, render only `unknown, OSV unreachable`; on known results, never call the rebuild “CVE-free,” never hardcode CVE-2018-3750, and never infer an advisory from package name. Acceptance: preserve the current 10-second cap and no-network path for zero rebuilt dependencies, preserve advisory/CVE counts, and keep report generation successful when OSV is unavailable.
